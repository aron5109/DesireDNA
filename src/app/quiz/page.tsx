import type {Metadata} from "next";import {QuizExperience} from "@/components/QuizExperience";export const metadata:Metadata={title:"Private quiz",robots:{index:false,follow:false}};export default function Quiz(){return <main className="mx-auto min-h-[100svh] max-w-lg px-4"><QuizExperience/></main>}
