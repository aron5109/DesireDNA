import {NextResponse} from "next/server";import {z} from "zod";import {ownerCookie} from "@/lib/server/cookies";import {setShareMode} from "@/lib/server/profile-store";export const runtime="nodejs";export async function POST(req:Request){try{const {shareMode}=z.object({shareMode:z.enum(["mutual_only","full_comparison"])}).strict().parse(await req.json()),owner=await ownerCookie();if(!owner||!await setShareMode(owner,shareMode))return NextResponse.json({error:"No active profile."},{status:401,headers:{"Cache-Control":"no-store"}});return NextResponse.json({shareMode},{headers:{"Cache-Control":"no-store"}})}catch{return NextResponse.json({error:"Unable to update sharing."},{status:400,headers:{"Cache-Control":"no-store"}})}}
