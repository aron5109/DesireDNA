import {describe,expect,it} from "vitest";import {compareProfiles} from "@/lib/quiz/comparison";import {questions,QUIZ_VERSION} from "@/data/questions";import type {ProfilePayload,QuizAnswer,ShareMode} from "@/lib/quiz/types";
const q=questions[0];function p(answers:QuizAnswer[],shareMode:ShareMode="mutual_only"):ProfilePayload{return{answers,result:{adventureIndex:0,communicationScore:0,categoryScores:{},answered:0,skipped:0,hardLimits:0,personality:{name:"",emoji:"",description:""}},desireCode:"",tone:"playful",consentTimestamp:"",quizVersion:QUIZ_VERSION,retentionDays:7,shareMode}}
describe("comparison privacy",()=>{it("finds strong matches and curiosities",()=>{expect(compareProfiles(p([{questionId:q.id,value:"love_it"}]),p([{questionId:q.id,value:"want_to_try"}])).strongMatches).toContain(q.shortLabel);expect(compareProfiles(p([{questionId:q.id,value:"maybe_conditions"}]),p([{questionId:q.id,value:"want_to_try"}])).sharedCuriosities).toContain(q.shortLabel)});it("hides mismatch unless both opt in",()=>{const answersA=[{questionId:q.id,value:"love_it"}] as QuizAnswer[],answersB=[{questionId:q.id,value:"hard_limit"}] as QuizAnswer[];const hidden=compareProfiles(p(answersA),p(answersB,"full_comparison"));expect(hidden.boundaryMismatches).toBeUndefined();const full=compareProfiles(p(answersA,"full_comparison"),p(answersB,"full_comparison"));expect(full.boundaryMismatches).toContain(q.shortLabel)});it("returns adult media overlap only",()=>{const c=compareProfiles(p([{questionId:"porn_categories",value:["Romantic","Couples"]}]),p([{questionId:"porn_categories",value:["Romantic","BDSM"]}]));expect(c.mutualPornCategories).toEqual(["Romantic"])})});
