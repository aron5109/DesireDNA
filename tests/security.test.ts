import {describe,expect,it} from "vitest";import {randomBytes} from "node:crypto";import {decrypt,encrypt} from "@/lib/server/crypto";import {generateDesireCode,generateOwnerToken,normalizeDesireCode} from "@/lib/server/codes";
describe("security primitives",()=>{it("formats and normalizes securely generated codes",()=>{const values=new Set(Array.from({length:100},generateDesireCode));expect(values.size).toBe(100);for(const code of values){expect(code).toMatch(/^DDNA-(?:[A-HJ-NP-Z2-9]{4}-){3}[A-HJ-NP-Z2-9]{4}$/);expect(normalizeDesireCode(code.replaceAll("-","").toLowerCase())).toBe(code)}expect(generateOwnerToken().length).toBeGreaterThan(40)});it("round trips AES-GCM and rejects tampering",()=>{const key=randomBytes(32).toString("base64"),sealed=encrypt({private:"answer"},key);expect(decrypt(sealed,key)).toEqual({private:"answer"});expect(()=>decrypt({...sealed,ciphertext:"A"+sealed.ciphertext.slice(1)},key)).toThrow()})});
